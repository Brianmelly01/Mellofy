const window = Object.assign({}, globalThis);
const document = {};
const self = window;

const exportedVars = (function(g) {

  const rawValues = {
    "signatureTimestampVar": "20515"
  };

  return { rawValues };
})({});
